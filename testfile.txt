const int Mod = 10007;

const int Map[2][2] = {
        {1, 1},
        {1, 0}
}, Bound[2] = {2, 2};

int T, f[2] = {1, 0};
int ori[2][2] = {
        {1, 0},
        {0, 0}
};

int main() {
    T = 1050;
    while (T) {
        printf("T = %d\n", T);
        if (T / 2 * 2 != T) {
            printf("OK\n")
        } else {}
        printf("T = %d\n", T);
        T = T / 2;
        printf("T = %d\n", T);
    }
    return 0;
}